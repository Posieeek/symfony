{% extends 'base.html.twig' %}

{% block body %}
    <h1>Tworzenie produktu</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('product_index') }}">Wróć do listy produktów</a>
        </li>
    </ul>
{% endblock %}
